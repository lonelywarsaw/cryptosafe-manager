# CryptoSafe Manager — зависимости (Windows, Linux, macOS)

# Тестирование
pytest>=7.4,<9
pytest-cov>=4.1,<6

# Криптография
argon2-cffi>=23.1.0,<26
cryptography>=41.0,<45

# GUI (все платформы с графической средой)
PyQt6>=6.4,<7

# Только Windows
pywin32>=305; sys_platform == 'win32'
pyzbar>=0.1.9; sys_platform == 'win32'

# QR PNG / изображения
qrcode[pil]>=7.4,<9
Pillow>=10.0,<12

# Упаковка и утилиты
pyinstaller>=6.0,<7
pyautogui>=0.9.54,<0.10
ruff>=0.4,<0.10
